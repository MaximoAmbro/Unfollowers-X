javascript:(function(){'use strict';const CFG={UF_DELAY_MIN:5_115,UF_DELAY_MAX:32_160,UF_CD_EVERY:10,UF_CD_MIN:4 * 60 * 1_000,UF_CD_MAX:10 * 60 * 1_000,AF_DELAY_MIN:22_500,AF_DELAY_MAX:47_500,AF_CD_EVERY:10,AF_CD_MIN:4 * 60 * 1_000,AF_CD_MAX:10 * 60 * 1_000,SC_MIN:500,SC_MAX:2_000,SC_STEP:700,SC_MAX_STUCK:6,TIMEOUT:2 * 60 * 60 * 1_000,};const S={sessionStart:null,activeModule:null,running:false,stop:false,uf:{nonMutuals:[],selected:new Set(),count:0,phase:'idle',},af:{candidates:[],selected:new Set(),seen:new Set(),count:0,phase:'idle',scanDone:false,stuckCount:0,lastHeight:0,targetCount:250,},};const sleep=ms=> new Promise(r=> setTimeout(r,ms));const rnd=(a,b)=> Math.random()*(b - a)+a;const rndInt=(a,b)=> Math.floor(Math.random()*(b - a+1))+a;function esc(s){return String(s??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');}function uname(href){return href?(href.split('/')[1]??'').split('?')[0].toLowerCase()|| null:null;}function fmtMs(ms){const s=Math.ceil(ms / 1_000);const m=Math.floor(s / 60);return m > 0?`${m}m ${s % 60}s`:`${s}s`;}function fmtSec(ms){return(ms / 1_000).toFixed(2)+'s';}function icolor(ch){const p=['#1e3a5f','#1e4d3a','#3d2260','#5c2020','#1a4a4a','#3a3a1e','#2d1f4f','#1f3d2d'];return p[((ch || 'a').toLowerCase().charCodeAt(0)- 97+26)% 8];}function expired(){return S.sessionStart &&(Date.now()- S.sessionStart)> CFG.TIMEOUT;}function detectPage(){const path=window.location.pathname;if(/\/following\b/.test(path))return 'following';if(/\/followers\b/.test(path))return 'followers';return 'other';}const DETECT={_sys:new Set(['search','explore','notifications','messages','settings','home','i','following','followers']),followsBack(cell){const t=cell.innerText || cell.textContent || '';return /te sigue\b/i.test(t)|| /follows you\b/i.test(t);},userInfo(cell){let username=null;for(const a of cell.querySelectorAll('a[href^="/"]')){const u=uname(a.getAttribute('href'));if(u && !u.includes('#')&& !this._sys.has(u)){username=u;break;}}const ne=cell.querySelector('[data-testid="User-Name"] span span')|| cell.querySelector('[data-testid="UserName"] span span');return{username,displayName:ne?.textContent?.trim()|| username || '?'};},unfollowBtn(cell){return(cell.querySelector('[data-testid$="-follow"]')|| cell.querySelector('button[aria-label*="Unfollow" i]')|| cell.querySelector('button[aria-label*="Dejar de seguir" i]')|| [...cell.querySelectorAll('button')].find(b=> /siguiendo|following/i.test(b.textContent))|| null);},followBtn(cell){const byTestId=cell.querySelector('[data-testid$="-follow"]');if(byTestId){const lbl=byTestId.getAttribute('aria-label')|| '';if(!/unfollow/i.test(lbl))return byTestId;}return(cell.querySelector('button[aria-label^="Follow @" i]')|| [...cell.querySelectorAll('button')].find(b=>{const t=b.textContent?.trim()|| '';const l=b.getAttribute('aria-label')|| '';return /^follow$/i.test(t)&& !/following|unfollow/i.test(t+l);})|| null);},};const SCRAPER={_buildEndDetector(getLoaded,targetCount,labelId){let listEnd=false;let isValidating=false;const endDetected=new Promise(resolve=>{let observer=null;let validationTimer=null;let interval=null;const cleanup=()=>{if(observer){observer.disconnect();observer=null;}if(validationTimer){clearTimeout(validationTimer);validationTimer=null;}if(interval){clearInterval(interval);interval=null;}};const confirmEnd=()=>{cleanup();listEnd=true;isValidating=false;resolve();};const tryAgain=()=>{isValidating=false;updateObserver();};const performValidationScroll=()=>{const before=window.scrollY;window.scrollBy(0,window.innerHeight / 2);setTimeout(()=>{const afterDown=window.scrollY;window.scrollBy(0,-(window.innerHeight / 2));setTimeout(()=>{Math.abs(afterDown - before)< 5?confirmEnd():tryAgain();},500);},500);};const startValidation=countBefore=>{if(validationTimer)clearTimeout(validationTimer);const sub=document.getElementById(labelId);if(sub)sub.textContent='Validando si hay mas usuarios...';validationTimer=setTimeout(()=>{const countAfter=document.querySelectorAll('[data-testid="UserCell"]').length;countAfter===countBefore?performValidationScroll():tryAgain();},5_000);};const updateObserver=()=>{if(isValidating || S.stop)return;if(targetCount !==null && getLoaded()>=targetCount)return;if(observer)observer.disconnect();const cells=document.querySelectorAll('[data-testid="UserCell"]');if(!cells.length)return;observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting && !isValidating){isValidating=true;startValidation(document.querySelectorAll('[data-testid="UserCell"]').length);}});},{root:null,rootMargin:'0px',threshold:0.5});observer.observe(cells[cells.length - 1]);};updateObserver();interval=setInterval(()=>{if(S.stop || listEnd){cleanup();resolve();return;}if(targetCount !==null && getLoaded()>=targetCount){cleanup();resolve();return;}updateObserver();},500);});return{endDetected,isValidating:()=> isValidating,isListEnd:()=> listEnd,};},async scanUnfollowers(onProgress){window.scrollTo(0,0);await sleep(1_200);const seen=new Set();const detector=this._buildEndDetector(()=> seen.size,null,'xuf-scan-sub');while(!detector.isListEnd()&& !S.stop){if(expired())break;document.querySelectorAll('[data-testid="UserCell"]').forEach(cell=>{const{username,displayName}=DETECT.userInfo(cell);if(!username || seen.has(username))return;seen.add(username);if(!DETECT.followsBack(cell))S.uf.nonMutuals.push({username,displayName});});onProgress(seen.size,S.uf.nonMutuals.length);if(!detector.isValidating())window.scrollBy(0,CFG.SC_STEP);await sleep(rndInt(CFG.SC_MIN,CFG.SC_MAX));}await detector.endDetected;},async scanFollowers(targetCount,onProgress){let loaded=S.af.candidates.length;const detector=this._buildEndDetector(()=> loaded,targetCount,'xuf-af-sub');while(loaded < targetCount && !detector.isListEnd()&& !S.stop){if(expired())break;document.querySelectorAll('[data-testid="UserCell"]').forEach(cell=>{if(loaded >=targetCount)return;const{username,displayName}=DETECT.userInfo(cell);if(!username || S.af.seen.has(username))return;if(!DETECT.followBtn(cell))return;S.af.seen.add(username);S.af.candidates.push({username,displayName});loaded++;});onProgress(loaded,targetCount);if(loaded >=targetCount)break;if(!detector.isValidating())window.scrollBy(0,CFG.SC_STEP);await sleep(rndInt(CFG.SC_MIN,CFG.SC_MAX));}await detector.endDetected;S.af.scanDone=loaded < targetCount && detector.isListEnd();return loaded;},};const UF={async waitConfirm(t=4_000){const d=Date.now()+t;return new Promise(r=>{const ck=()=>{const b=document.querySelector('[data-testid="confirmationSheetConfirm"]');if(b){b.click();return r(true);}Date.now()< d?setTimeout(ck,150):r(true);};ck();});},async countdown(ms,onTick){const deadline=Date.now()+ms;while(Date.now()< deadline && !S.stop){onTick(deadline - Date.now());await sleep(500);}},async run(queue,onProgress,onCooldown){S.uf.count=0;window.scrollTo(0,0);await sleep(1_500);const pending=new Set(queue.map(u=> u.username));let done=0,lastH=0,stuck=0;while(pending.size > 0 && !S.stop && stuck < 7){if(expired())break;const cells=document.querySelectorAll('[data-testid="UserCell"]');let found=false;for(const cell of cells){const{username}=DETECT.userInfo(cell);if(!username || !pending.has(username))continue;found=true;pending.delete(username);cell.scrollIntoView({behavior:'smooth',block:'center'});await sleep(500+Math.random()* 400);const btn=DETECT.unfollowBtn(cell);if(!btn){console.warn('[XUF] btn no encontrado @'+username);continue;}const delayMs=rnd(CFG.UF_DELAY_MIN,CFG.UF_DELAY_MAX);onProgress(done+1,queue.length,username,delayMs);try{btn.dispatchEvent(new MouseEvent('mouseover',{bubbles:true,cancelable:true}));await sleep(250+Math.random()* 150);btn.click();await sleep(800+Math.random()* 300);await this.waitConfirm();await sleep(400);S.uf.count++;done++;}catch(e){console.error('[XUF] error @'+username,e);}if(done > 0 && done % CFG.UF_CD_EVERY===0 && pending.size > 0 && !S.stop){const cdMs=rndInt(CFG.UF_CD_MIN,CFG.UF_CD_MAX);await this.countdown(cdMs,rem=> onCooldown(rem,done,queue.length));}else if(pending.size > 0 && !S.stop){await this.countdown(delayMs,rem=> onProgress(done,queue.length,username,rem));}}if(!found){window.scrollBy(0,CFG.SC_STEP);await sleep(rndInt(CFG.SC_MIN,CFG.SC_MAX));const nh=document.body.scrollHeight;stuck=nh===lastH?stuck+1:0;lastH=nh;}else{stuck=0;}}return{done,stopped:S.stop};},};const AF={async waitConfirm(t=4_000){const d=Date.now()+t;return new Promise(r=>{const ck=()=>{const b=document.querySelector('[data-testid="confirmationSheetConfirm"]');if(b){b.click();return r(true);}Date.now()< d?setTimeout(ck,150):r(true);};ck();});},async countdown(ms,onTick){const deadline=Date.now()+ms;while(Date.now()< deadline && !S.stop){onTick(deadline - Date.now());await sleep(500);}},async run(queue,onProgress,onCooldown){S.af.count=0;window.scrollTo(0,0);await sleep(1_500);const pending=new Set(queue.map(u=> u.username));let done=0,lastH=0,stuck=0;while(pending.size > 0 && !S.stop && stuck < 7){if(expired())break;const cells=document.querySelectorAll('[data-testid="UserCell"]');let found=false;for(const cell of cells){const{username}=DETECT.userInfo(cell);if(!username || !pending.has(username))continue;found=true;pending.delete(username);cell.scrollIntoView({behavior:'smooth',block:'center'});await sleep(500+Math.random()* 400);const btn=DETECT.followBtn(cell);if(!btn){console.warn('[XUF] follow btn no encontrado @'+username);continue;}const delayMs=rndInt(CFG.AF_DELAY_MIN,CFG.AF_DELAY_MAX);onProgress(done+1,queue.length,username,delayMs);try{btn.dispatchEvent(new MouseEvent('mouseover',{bubbles:true,cancelable:true}));await sleep(250+Math.random()* 150);btn.click();await sleep(800+Math.random()* 300);await this.waitConfirm();await sleep(400);S.af.count++;done++;}catch(e){console.error('[XUF] error follow @'+username,e);}if(pending.size===0 || S.stop)break;if(done % CFG.AF_CD_EVERY===0){const cdMs=rndInt(CFG.AF_CD_MIN,CFG.AF_CD_MAX);await this.countdown(cdMs,rem=> onCooldown(rem,done,queue.length));}else{await this.countdown(delayMs,rem=> onProgress(done,queue.length,username,rem));}}if(!found){window.scrollBy(0,CFG.SC_STEP);await sleep(rndInt(CFG.SC_MIN,CFG.SC_MAX));const nh=document.body.scrollHeight;stuck=nh===lastH?stuck+1:0;lastH=nh;}else{stuck=0;}}return{done,stopped:S.stop};},};const UI={_el:null,_CSS:` #xuf-ov{position:fixed;inset:0;z-index:9999999;background:#111111;color:#d8d8d8;font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,'Helvetica Neue',Arial,sans-serif;font-size:14px;line-height:1.55;display:flex;flex-direction:column;overflow:hidden;}@keyframes xuf-in{from{opacity:0;transform:translateY(8px);}to{opacity:1;transform:translateY(0);}}.xuf-anim{animation:xuf-in 0.2s ease;}.xuf-nav{display:flex;flex-direction:column;border-bottom:1px solid #1e1e1e;background:#0d0d0d;flex-shrink:0;align-items:stretch;}.xuf-nav-top{display:flex;align-items:center;padding:12px 24px 8px;gap:16px;}.xuf-brand{flex:1;text-align:center;font-size:15px;font-weight:700;letter-spacing:0.5px;color:#c8c8c8;white-space:nowrap;}.xuf-ver{color:#585858;}.xuf-tabs{display:flex;gap:4px;justify-content:center;padding:0 24px 10px;}.xuf-tab{background:transparent;border:1px solid transparent;border-radius:5px;padding:6px 16px;font-size:12px;font-weight:600;color:#383838;cursor:pointer;transition:all 0.15s;font-family:inherit;white-space:nowrap;}.xuf-tab:hover:not(.xuf-tab-disabled):not(.xuf-tab-active){border-color:#222;color:#686868;}.xuf-tab.xuf-tab-active{background:#1a1a1a;border-color:#282828;color:#c8c8c8;}.xuf-tab.xuf-tab-disabled{opacity:0.28;cursor:not-allowed;}.xuf-tab-hint{font-size:11px;color:#2a2a2a;text-align:center;flex:1;}.xuf-close{background:none;border:none;cursor:pointer;font-size:11px;font-weight:700;color:#282828;padding:6px 8px;border-radius:4px;transition:color 0.1s,background 0.1s;font-family:inherit;white-space:nowrap;}.xuf-close:hover{color:#c8c8c8;background:#1a1a1a;}.xuf-body{flex:1;overflow:hidden;display:flex;flex-direction:column;}.xuf-body-center{align-items:center;justify-content:center;overflow-y:auto;}.xuf-body-panel{align-items:stretch;justify-content:flex-start;overflow:hidden;}.xuf-card{display:flex;flex-direction:column;align-items:center;gap:24px;width:100%;max-width:440px;padding:52px 32px;text-align:center;}@keyframes xuf-spin{to{transform:rotate(360deg);}}.xuf-spinner{width:28px;height:28px;border:2px solid #1a1a1a;border-top-color:#555;border-radius:50%;animation:xuf-spin 0.85s linear infinite;flex-shrink:0;}.xuf-heading{font-size:19px;font-weight:600;color:#d8d8d8;margin:0;letter-spacing:-0.3px;}.xuf-sub{font-size:13px;color:#404040;margin:0;max-width:340px;line-height:1.65;}.xuf-big-num{font-size:76px;font-weight:700;color:#d8d8d8;line-height:1;margin:0;letter-spacing:-4px;}.xuf-target{font-size:16px;font-weight:600;color:#585858;margin:0;min-height:22px;}.xuf-page-note{font-size:12px;color:#2e2e2e;margin:0;text-align:center;}.xuf-page-ind{display:flex;flex-direction:column;align-items:center;gap:8px;padding:20px 28px;background:#0d0d0d;border:1px solid #181818;border-radius:8px;width:100%;max-width:400px;}.xuf-page-ind-url{font-size:11px;font-family:'SF Mono',Consolas,monospace;color:#303030;}.xuf-page-ind-status{font-size:13px;color:#585858;}.xuf-page-ind-status.ok{color:#406840;}.xuf-module-warn{background:#0f0e00;border:1px solid #1c1a00;border-radius:6px;padding:10px 16px;font-size:12px;color:#504a00;width:100%;max-width:400px;text-align:center;}.xuf-module-warn strong{color:#807030;}.xuf-stats{display:flex;gap:48px;align-items:flex-end;}.xuf-stat{display:flex;flex-direction:column;align-items:center;gap:4px;}.xuf-stat-n{font-size:40px;font-weight:700;color:#d8d8d8;line-height:1;letter-spacing:-1.5px;min-width:56px;text-align:center;}.xuf-stat-l{font-size:9px;color:#282828;text-transform:uppercase;letter-spacing:1.2px;}.xuf-prog-wrap{width:100%;max-width:360px;display:flex;flex-direction:column;align-items:center;gap:10px;}.xuf-prog-track{width:100%;height:2px;background:#1a1a1a;border-radius:1px;overflow:hidden;}.xuf-prog-fill{height:100%;background:#d8d8d8;border-radius:1px;transition:width 0.4s ease;}.xuf-prog-lbl{font-size:11px;color:#303030;}.xuf-cd-banner{background:#0d0f16;border:1px solid #141820;border-radius:6px;padding:12px 20px;font-size:13px;color:#383c50;width:100%;max-width:360px;text-align:center;line-height:1.6;}.xuf-cd-banner strong{color:#5060a0;}.xuf-panel{display:flex;flex-direction:column;height:100%;max-width:900px;width:100%;margin:0 auto;}.xuf-panel-hd{display:flex;align-items:center;justify-content:space-between;padding:14px 28px;border-bottom:1px solid #181818;flex-shrink:0;gap:16px;}.xuf-panel-hd-r{display:flex;align-items:center;gap:18px;}.xuf-sel-lbl{font-size:12px;color:#383838;white-space:nowrap;}.xuf-sel-lbl strong{color:#b8b8b8;font-weight:600;}.xuf-limit-bar{background:#120f00;border-bottom:1px solid #1e1900;padding:7px 28px;font-size:12px;color:#504800;flex-shrink:0;}.xuf-limit-bar strong{color:#807030;}.xuf-toolbar{display:flex;align-items:center;gap:8px;padding:10px 28px;border-bottom:1px solid #181818;flex-shrink:0;}.xuf-table-scroll{flex:1;overflow-y:auto;scrollbar-width:thin;scrollbar-color:#1a1a1a transparent;}.xuf-table-scroll::-webkit-scrollbar{width:3px;}.xuf-table-scroll::-webkit-scrollbar-thumb{background:#1a1a1a;}.xuf-table{width:100%;border-collapse:collapse;}.xuf-tr{border-bottom:1px solid #131313;transition:background 0.1s;}.xuf-tr:hover{background:#0f0f0f;}.xuf-td{padding:9px 12px;vertical-align:middle;}.xuf-td-cb{width:44px;text-align:center;}.xuf-td-av{width:48px;}.xuf-avatar{width:32px;height:32px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700;color:rgba(255,255,255,0.6);flex-shrink:0;}.xuf-u-name{display:block;font-weight:600;font-size:13px;color:#c8c8c8;}.xuf-u-handle{display:block;font-size:11px;color:#383838;margin-top:2px;}.xuf-panel-ft{display:flex;align-items:center;justify-content:space-between;padding:14px 28px;border-top:1px solid #181818;flex-shrink:0;gap:12px;flex-wrap:wrap;}.xuf-ft-note{font-size:11px;color:#282828;margin:0;}.xuf-btn{padding:7px 18px;border-radius:4px;border:none;cursor:pointer;font-size:12px;font-weight:600;transition:background 0.15s,color 0.15s,opacity 0.15s;white-space:nowrap;font-family:inherit;line-height:1.4;}.xuf-btn:disabled{opacity:0.3;cursor:not-allowed;pointer-events:none;}.xuf-btn:focus-visible{outline:2px solid #484848;outline-offset:2px;}.xuf-btn-primary{background:#d8d8d8;color:#0d0d0d;}.xuf-btn-primary:hover:not(:disabled){background:#f0f0f0;}.xuf-btn-ghost{background:transparent;color:#484848;border:1px solid #1e1e1e;}.xuf-btn-ghost:hover:not(:disabled){background:#181818;color:#b8b8b8;border-color:#2a2a2a;}.xuf-btn-danger{background:#3a0f0f;color:#c87070;border:1px solid #4a1515;}.xuf-btn-danger:hover:not(:disabled){background:#4a1515;color:#e08080;}.xuf-cb{width:13px;height:13px;cursor:pointer;accent-color:#b8b8b8;}.xuf-cb:disabled{opacity:0.3;cursor:not-allowed;}.xuf-config-wrap{display:flex;flex-direction:column;align-items:center;gap:14px;width:100%;max-width:340px;}.xuf-num-input{width:100%;background:#0d0d0d;border:1px solid #252525;border-radius:4px;padding:10px 14px;font-size:26px;font-weight:700;color:#d8d8d8;text-align:center;font-family:inherit;outline:none;transition:border-color 0.15s;-moz-appearance:textfield;}.xuf-num-input::-webkit-outer-spin-button,.xuf-num-input::-webkit-inner-spin-button{-webkit-appearance:none;margin:0;}.xuf-num-input:focus{border-color:#3a3a3a;}.xuf-slider{width:100%;-webkit-appearance:none;appearance:none;height:2px;background:#252525;border-radius:1px;outline:none;cursor:pointer;}.xuf-slider::-webkit-slider-thumb{-webkit-appearance:none;width:16px;height:16px;border-radius:50%;background:#c0c0c0;cursor:pointer;transition:background 0.15s;}.xuf-slider::-webkit-slider-thumb:hover{background:#f0f0f0;}.xuf-slider::-moz-range-thumb{width:16px;height:16px;border-radius:50%;background:#c0c0c0;cursor:pointer;border:none;}.xuf-range-labels{display:flex;justify-content:space-between;width:100%;font-size:10px;color:#282828;}.xuf-estimate{font-size:13px;font-weight:600;color:#505050;margin:0;text-align:center;min-height:20px;}`,injectCSS(){if(document.getElementById('xuf-css'))return;const s=document.createElement('style');s.id='xuf-css';s.textContent=this._CSS;document.head.appendChild(s);},mount(){document.getElementById('xuf-ov')?.remove();this.injectCSS();this._el=document.createElement('div');this._el.id='xuf-ov';document.body.appendChild(this._el);return this._el;},unmount(){this._el?.remove();this._el=null;},buildShell(pageType){if(!this._el)return;const ufDisabled=pageType !=='following';const afDisabled=pageType !=='followers';this._el.innerHTML=` <div class="xuf-nav"> <div class="xuf-nav-top"> <span class="xuf-brand">Unfollowers-X <span class="xuf-ver">V2.2</span></span> <button class="xuf-close" id="xuf-close-btn">Cerrar</button> </div> <div class="xuf-tabs"> <button class="xuf-tab ${ufDisabled?'xuf-tab-disabled':'xuf-tab-active'}" id="xuf-tab-uf" ${ufDisabled?'disabled':''}>Modulo Unfollower</button> <button class="xuf-tab ${afDisabled?'xuf-tab-disabled':''}${!afDisabled?'xuf-tab-active':''}" id="xuf-tab-af" ${afDisabled?'disabled':''}>Modulo Auto-Follow</button> </div> </div> <div class="xuf-body xuf-body-center" id="xuf-body"></div> `;document.getElementById('xuf-close-btn').onclick=()=>{if(S.running){S.stop=true;}this.unmount();};document.getElementById('xuf-tab-uf').onclick=()=>{if(ufDisabled)return;if(S.running){this._showModuleWarn();return;}CTRL.startUnfollower();};document.getElementById('xuf-tab-af').onclick=()=>{if(afDisabled)return;if(S.running){this._showModuleWarn();return;}CTRL.startAutoFollow();};},_showModuleWarn(){const body=this._body();if(!body)return;const mod=S.activeModule==='unfollow'?'Unfollower':'Auto-Follow';const existing=body.querySelector('.xuf-module-warn');if(existing){existing.remove();return;}const w=document.createElement('div');w.className='xuf-module-warn xuf-anim';w.innerHTML=`El modulo <strong>${mod}</strong> esta en ejecucion. Completa o detiene el proceso antes de cambiar de modulo.`;body.appendChild(w);setTimeout(()=> w.remove(),3500);},_body(){return document.getElementById('xuf-body');},_setBodyMode(mode){const b=this._body();if(!b)return;b.className='xuf-body '+(mode==='panel'?'xuf-body-panel':'xuf-body-center');},showDashboard(pageType){this._setBodyMode('center');const body=this._body();if(!body)return;let content;if(pageType==='following'){content=` <div class="xuf-page-ind xuf-anim"> <span class="xuf-page-ind-url">${esc(window.location.pathname)}</span> <span class="xuf-page-ind-status ok">Pagina /following detectada</span> </div> <p class="xuf-heading">Modulo Unfollower disponible</p> <p class="xuf-sub">Detectara todas las cuentas que no te siguen de vuelta y te permitira dejar de seguirlas con delays precisos.</p> <button class="xuf-btn xuf-btn-primary" id="xuf-start-uf">Iniciar Modulo Unfollower</button> `;}else if(pageType==='followers'){content=` <div class="xuf-page-ind xuf-anim"> <span class="xuf-page-ind-url">${esc(window.location.pathname)}</span> <span class="xuf-page-ind-status ok">Pagina /followers detectada</span> </div> <p class="xuf-heading">Modulo Auto-Follow disponible</p> <p class="xuf-sub">Elige cuantos seguidores cargar(1-5000)y siguelos automaticamente con delays anti-baneo.</p> <button class="xuf-btn xuf-btn-primary" id="xuf-start-af"> Iniciar Modulo Auto-Follow </button> `;}else{content=` <div class="xuf-page-ind xuf-anim"> <span class="xuf-page-ind-url">${esc(window.location.pathname)}</span> <span class="xuf-page-ind-status">Pagina no reconocida</span> </div> <p class="xuf-heading">Navega a la pagina correcta</p> <div class="xuf-sub" style="text-align:left;max-width:380px"> <p style="margin-bottom:8px"><strong style="color:#888">Modulo Unfollower:</strong><br>x.com/TU_USUARIO/following</p> <p><strong style="color:#888">Modulo Auto-Follow:</strong><br>x.com/@USUARIO/followers</p> </div> <button class="xuf-btn xuf-btn-ghost" id="xuf-redetect">Re-detectar pagina</button> `;}body.innerHTML=`<div class="xuf-card xuf-anim">${content}</div>`;document.getElementById('xuf-start-uf')?.addEventListener('click',()=> CTRL.startUnfollower());document.getElementById('xuf-start-af')?.addEventListener('click',()=> CTRL.startAutoFollow());document.getElementById('xuf-redetect')?.addEventListener('click',()=> CTRL.init());},uf:{showScan(){UI._setBodyMode('center');const b=UI._body();if(!b)return;b.innerHTML=` <div class="xuf-card xuf-anim"> <div class="xuf-spinner"></div> <p class="xuf-heading">Escaneando lista de seguidos</p> <p class="xuf-sub" id="xuf-scan-sub">Iniciando escaneo...</p> <div class="xuf-stats"> <div class="xuf-stat"> <span class="xuf-stat-n" id="xuf-sc-n">0</span> <span class="xuf-stat-l">Revisados</span> </div> <div class="xuf-stat"> <span class="xuf-stat-n" id="xuf-sc-f">0</span> <span class="xuf-stat-l">No-mutuos</span> </div> </div> <button class="xuf-btn xuf-btn-ghost" id="xuf-uf-cancel">Cancelar</button> </div> `;document.getElementById('xuf-uf-cancel').onclick=()=>{S.stop=true;};},updateScan(n,found){const sub=document.getElementById('xuf-scan-sub');const nn=document.getElementById('xuf-sc-n');const nf=document.getElementById('xuf-sc-f');if(sub)sub.textContent='Escaneando usuario '+n+'...';if(nn)nn.textContent=n;if(nf)nf.textContent=found;},showResults(nonMutuals){UI._setBodyMode('panel');const b=UI._body();if(!b)return;const rows=nonMutuals.map(({username,displayName})=>{const ini=(displayName[0] || username[0] || '?').toUpperCase();const color=icolor(ini);const chk=S.uf.selected.has(username)?'checked':'';return ` <tr class="xuf-tr"> <td class="xuf-td xuf-td-cb"> <input type="checkbox" class="xuf-cb" data-u="${esc(username)}" ${chk}/> </td> <td class="xuf-td xuf-td-av"> <div class="xuf-avatar" style="background:${color}">${esc(ini)}</div> </td> <td class="xuf-td"> <span class="xuf-u-name">${esc(displayName)}</span> <span class="xuf-u-handle">@${esc(username)}</span> </td> </tr> `;}).join('');b.innerHTML=` <div class="xuf-panel xuf-anim"> <div class="xuf-panel-hd"> <span class="xuf-sub">No-mutuos detectados</span> <div class="xuf-panel-hd-r"> <span class="xuf-sel-lbl"> <strong id="xuf-uf-sel-n">${nonMutuals.length}</strong> de ${nonMutuals.length}seleccionados </span> </div> </div> <div class="xuf-toolbar"> <button class="xuf-btn xuf-btn-ghost" id="xuf-uf-sel-all">Seleccionar todo</button> <button class="xuf-btn xuf-btn-ghost" id="xuf-uf-desel">Deseleccionar todo</button> </div> <div class="xuf-table-scroll"> <table class="xuf-table"><tbody>${rows}</tbody></table> </div> <div class="xuf-panel-ft"> <p class="xuf-ft-note">Sin limite de unfollows por sesion — cooldown cada ${CFG.UF_CD_EVERY}acciones</p> <button class="xuf-btn xuf-btn-danger" id="xuf-uf-run"> Dejar de seguir seleccionados </button> </div> </div> `;S.uf.selected.clear();nonMutuals.forEach(u=> S.uf.selected.add(u.username));const refreshCnt=(total)=>{const el=document.getElementById('xuf-uf-sel-n');if(el)el.textContent=S.uf.selected.size;const btn=document.getElementById('xuf-uf-run');if(btn)btn.disabled=S.uf.selected.size===0;};b.querySelectorAll('.xuf-cb').forEach(cb=>{cb.addEventListener('change',()=>{cb.checked?S.uf.selected.add(cb.dataset.u):S.uf.selected.delete(cb.dataset.u);refreshCnt(nonMutuals.length);});});document.getElementById('xuf-uf-sel-all').onclick=()=>{b.querySelectorAll('.xuf-cb').forEach(cb=>{cb.checked=true;S.uf.selected.add(cb.dataset.u);});refreshCnt(nonMutuals.length);};document.getElementById('xuf-uf-desel').onclick=()=>{b.querySelectorAll('.xuf-cb').forEach(cb=>{cb.checked=false;S.uf.selected.delete(cb.dataset.u);});refreshCnt(nonMutuals.length);};document.getElementById('xuf-uf-run').onclick=()=>{if(S.uf.selected.size > 0)CTRL.runUnfollow();};},showRunning(total){UI._setBodyMode('center');const b=UI._body();if(!b)return;b.innerHTML=` <div class="xuf-card xuf-anim"> <p class="xuf-heading" id="xuf-uf-h">Preparando...</p> <p class="xuf-target" id="xuf-uf-u"></p> <div class="xuf-prog-wrap"> <div class="xuf-prog-track"> <div class="xuf-prog-fill" id="xuf-uf-prog" style="width:0%"></div> </div> <span class="xuf-prog-lbl" id="xuf-uf-lbl">0 / ${total}</span> </div> <div class="xuf-cd-banner" id="xuf-uf-cd" style="display:none"></div> <button class="xuf-btn xuf-btn-ghost" id="xuf-uf-stop">Detener</button> </div> `;document.getElementById('xuf-uf-stop').onclick=()=>{S.stop=true;const b2=document.getElementById('xuf-uf-stop');if(b2){b2.textContent='Deteniendo...';b2.disabled=true;}};},updateProgress(cur,total,username,remMs){const pct=Math.round((cur / total)* 100);const h=document.getElementById('xuf-uf-h');const u=document.getElementById('xuf-uf-u');const p=document.getElementById('xuf-uf-prog');const l=document.getElementById('xuf-uf-lbl');const cd=document.getElementById('xuf-uf-cd');if(h)h.textContent='Dejando de seguir...';if(u)u.textContent='@'+username;if(p)p.style.width=pct+'%';if(l)l.textContent=cur+' / '+total+' — Espera '+fmtSec(remMs);if(cd)cd.style.display='none';},updateCooldown(remMs,cur,total){const h=document.getElementById('xuf-uf-h');const u=document.getElementById('xuf-uf-u');const cd=document.getElementById('xuf-uf-cd');if(h)h.textContent='Cooldown activo';if(u)u.textContent=cur+' de '+total+' completados';if(cd){cd.style.display='block';cd.innerHTML=`Reanudando en <strong>${fmtMs(remMs)}</strong>`;}},showDone(count,stopped){UI._setBodyMode('center');const b=UI._body();if(!b)return;const msg=stopped?'Proceso detenido manualmente.':`Unfollowados ${count}usuario${count !==1?'s':''}con exito.`;b.innerHTML=` <div class="xuf-card xuf-anim"> ${count > 0?`<p class="xuf-big-num">${count}</p>`:''}<p class="xuf-heading">Completado</p> <p class="xuf-sub">${esc(msg)}</p> <div style="display:flex;gap:8px"> <button class="xuf-btn xuf-btn-ghost" id="xuf-uf-again">Ejecutar nuevamente</button> <button class="xuf-btn xuf-btn-primary" id="xuf-uf-dash">Volver al Dashboard</button> </div> </div> `;document.getElementById('xuf-uf-again').onclick=()=> CTRL.startUnfollower();document.getElementById('xuf-uf-dash').onclick=()=> CTRL.backToDashboard();},},af:{showConfig(){UI._setBodyMode('center');const b=UI._body();if(!b)return;const DEFAULT=S.af.targetCount || 250;b.innerHTML=` <div class="xuf-card xuf-anim" style="max-width:480px"> <p class="xuf-heading">Configuracion</p> <p class="xuf-sub">Cuantos seguidores deseas cargar y seguir:</p> <div class="xuf-config-wrap"> <input type="number" id="xuf-af-cnt" class="xuf-num-input" value="${DEFAULT}" min="1" max="5000" /> <input type="range" id="xuf-af-slider" class="xuf-slider" value="${DEFAULT}" min="1" max="5000" step="1" /> <div class="xuf-range-labels"> <span>1</span> <span>5000</span> </div> <p class="xuf-estimate" id="xuf-af-est"></p> </div> <div style="display:flex;gap:8px"> <button class="xuf-btn xuf-btn-ghost" id="xuf-af-cfg-back">Volver</button> <button class="xuf-btn xuf-btn-primary" id="xuf-af-cfg-load">Cargar seguidores</button> </div> </div> `;const input=document.getElementById('xuf-af-cnt');const slider=document.getElementById('xuf-af-slider');const est=document.getElementById('xuf-af-est');function calcEstimate(n){const total=n * 35+Math.floor(n / 10)* 7 * 60;const h=Math.floor(total / 3600);const m=Math.floor((total % 3600)/ 60);return `${n}usuarios=${h}h ${m}m`;}function syncFrom(val){const n=Math.max(1,Math.min(5000,parseInt(val)|| 1));input.value=n;slider.value=n;est.textContent=calcEstimate(n);return n;}syncFrom(DEFAULT);input.addEventListener('input',()=> syncFrom(input.value));slider.addEventListener('input',()=> syncFrom(slider.value));input.addEventListener('keydown',e=>{const allowed=['Backspace','Delete','ArrowLeft','ArrowRight','Tab'];if(!allowed.includes(e.key)&& !/^\d$/.test(e.key))e.preventDefault();});document.getElementById('xuf-af-cfg-back').onclick=()=> CTRL.backToDashboard();document.getElementById('xuf-af-cfg-load').onclick=()=>{const count=Math.max(1,Math.min(5000,parseInt(input.value)|| 250));CTRL.loadFollowers(count);};},showLoading(count,target){UI._setBodyMode('center');const b=UI._body();if(!b)return;b.innerHTML=` <div class="xuf-card xuf-anim"> <div class="xuf-spinner"></div> <p class="xuf-heading">Cargando seguidores</p> <p class="xuf-sub" id="xuf-af-sub">Iniciando...</p> <div class="xuf-prog-wrap"> <div class="xuf-prog-track"> <div class="xuf-prog-fill" id="xuf-af-load-bar" style="width:0%"></div> </div> <span class="xuf-prog-lbl" id="xuf-af-load-lbl">${count}/ ${target}</span> </div> <button class="xuf-btn xuf-btn-ghost" id="xuf-af-cancel">Cancelar</button> </div> `;document.getElementById('xuf-af-cancel').onclick=()=>{S.stop=true;};},updateLoading(count,target){const sub=document.getElementById('xuf-af-sub');const bar=document.getElementById('xuf-af-load-bar');const lbl=document.getElementById('xuf-af-load-lbl');const pct=target > 0?Math.min(100,Math.round((count / target)* 100)):0;if(sub)sub.textContent='Cargando usuario '+count+' de '+target+'...';if(bar)bar.style.width=pct+'%';if(lbl)lbl.textContent=count+' / '+target;},showResults(candidates,requested){UI._setBodyMode('panel');const b=UI._body();if(!b)return;const headerMsg=(requested && requested > candidates.length)?`Cargados ${candidates.length}usuarios de ${requested}solicitados`:'Seguidores listos para seguir';const rows=candidates.map(({username,displayName})=>{const ini=(displayName[0] || username[0] || '?').toUpperCase();const color=icolor(ini);const chk=S.af.selected.has(username)?'checked':'';return ` <tr class="xuf-tr"> <td class="xuf-td xuf-td-cb"> <input type="checkbox" class="xuf-cb" data-u="${esc(username)}" ${chk}/> </td> <td class="xuf-td xuf-td-av"> <div class="xuf-avatar" style="background:${color}">${esc(ini)}</div> </td> <td class="xuf-td"> <span class="xuf-u-name">${esc(displayName)}</span> <span class="xuf-u-handle">@${esc(username)}</span> </td> </tr> `;}).join('');b.innerHTML=` <div class="xuf-panel xuf-anim"> <div class="xuf-panel-hd"> <span class="xuf-sub">${headerMsg}</span> <div class="xuf-panel-hd-r"> <span class="xuf-sel-lbl"> <strong id="xuf-af-sel-n">${candidates.length}</strong> usuarios seleccionados de ${candidates.length}cargados </span> </div> </div> <div class="xuf-toolbar"> <button class="xuf-btn xuf-btn-ghost" id="xuf-af-sel-all">Seleccionar todo</button> <button class="xuf-btn xuf-btn-ghost" id="xuf-af-desel">Deseleccionar todo</button> </div> <div class="xuf-table-scroll"> <table class="xuf-table"><tbody>${rows}</tbody></table> </div> <div class="xuf-panel-ft"> <p class="xuf-ft-note">Delays 22.5-47.5s — Cooldown cada ${CFG.AF_CD_EVERY}follows:4-10 min</p> <button class="xuf-btn xuf-btn-primary" id="xuf-af-run"> Ejecutar Follows </button> </div> </div> `;S.af.selected.clear();candidates.forEach(u=> S.af.selected.add(u.username));const refreshCnt=()=>{const el=document.getElementById('xuf-af-sel-n');const btn=document.getElementById('xuf-af-run');if(el)el.textContent=S.af.selected.size;if(btn)btn.disabled=S.af.selected.size===0;};b.querySelectorAll('.xuf-cb').forEach(cb=>{cb.addEventListener('change',()=>{cb.checked?S.af.selected.add(cb.dataset.u):S.af.selected.delete(cb.dataset.u);refreshCnt();});});document.getElementById('xuf-af-sel-all').onclick=()=>{b.querySelectorAll('.xuf-cb').forEach(cb=>{cb.checked=true;S.af.selected.add(cb.dataset.u);});refreshCnt();};document.getElementById('xuf-af-desel').onclick=()=>{b.querySelectorAll('.xuf-cb').forEach(cb=>{cb.checked=false;S.af.selected.delete(cb.dataset.u);});refreshCnt();};document.getElementById('xuf-af-run').onclick=()=>{if(S.af.selected.size > 0)CTRL.runAutoFollow();};},showRunning(total){UI._setBodyMode('center');const b=UI._body();if(!b)return;b.innerHTML=` <div class="xuf-card xuf-anim"> <p class="xuf-heading" id="xuf-af-h">Preparando...</p> <p class="xuf-target" id="xuf-af-u"></p> <div class="xuf-prog-wrap"> <div class="xuf-prog-track"> <div class="xuf-prog-fill" id="xuf-af-prog" style="width:0%"></div> </div> <span class="xuf-prog-lbl" id="xuf-af-lbl">0 / ${total}</span> </div> <div class="xuf-cd-banner" id="xuf-af-cd" style="display:none"></div> <button class="xuf-btn xuf-btn-ghost" id="xuf-af-stop">Cancelar</button> </div> `;document.getElementById('xuf-af-stop').onclick=()=>{S.stop=true;const s=document.getElementById('xuf-af-stop');if(s){s.textContent='Cancelando...';s.disabled=true;}};},updateProgress(cur,total,username,remMs){const pct=Math.round((cur / total)* 100);const h=document.getElementById('xuf-af-h');const u=document.getElementById('xuf-af-u');const p=document.getElementById('xuf-af-prog');const l=document.getElementById('xuf-af-lbl');const cd=document.getElementById('xuf-af-cd');if(h)h.textContent='Siguiendo usuario '+cur+' de '+total+'...';if(u)u.textContent='@'+username;if(p)p.style.width=pct+'%';if(l)l.textContent='Espera '+Math.ceil(remMs / 1_000)+'s';if(cd)cd.style.display='none';},updateCooldown(remMs,cur,total){const h=document.getElementById('xuf-af-h');const u=document.getElementById('xuf-af-u');const cd=document.getElementById('xuf-af-cd');if(h)h.textContent='Cooldown activo';if(u)u.textContent=cur+' de '+total+' completados';if(cd){cd.style.display='block';cd.innerHTML=`Reanudando en <strong>${fmtMs(remMs)}</strong>`;}},showDone(count,stopped){UI._setBodyMode('center');const b=UI._body();if(!b)return;const msg=stopped?`Cancelado. ${count}usuario${count !==1?'s':''}seguidos antes de parar.`:`Seguida a ${count}usuario${count !==1?'s':''}con exito.`;b.innerHTML=` <div class="xuf-card xuf-anim"> ${count > 0?`<p class="xuf-big-num">${count}</p>`:''}<p class="xuf-heading">${stopped?'Proceso cancelado':'Completado'}</p> <p class="xuf-sub">${esc(msg)}</p> <button class="xuf-btn xuf-btn-primary" id="xuf-af-dash">Volver al Dashboard</button> </div> `;document.getElementById('xuf-af-dash').onclick=()=> CTRL.backToDashboard();},},};const CTRL={init(){const page=detectPage();S.sessionStart=S.sessionStart || Date.now();S.running=false;S.stop=false;UI.buildShell(page);UI.showDashboard(page);},backToDashboard(){S.activeModule=null;S.running=false;S.stop=false;S.uf.phase='idle';S.af.phase='idle';this.init();},startUnfollower(){S.activeModule='unfollow';S.running=true;S.stop=false;S.uf.nonMutuals=[];S.uf.selected.clear();S.uf.count=0;S.uf.phase='scanning';document.getElementById('xuf-tab-uf')?.classList.add('xuf-tab-active');document.getElementById('xuf-tab-af')?.classList.remove('xuf-tab-active');UI.uf.showScan();this._doUnfollowerScan();},async _doUnfollowerScan(){await SCRAPER.scanUnfollowers((n,found)=> UI.uf.updateScan(n,found));if(S.stop){CTRL.backToDashboard();return;}if(S.uf.nonMutuals.length===0){S.running=false;UI.uf.showDone(0,false);return;}S.uf.phase='results';S.running=false;UI.uf.showResults(S.uf.nonMutuals);},runUnfollow(){const queue=S.uf.nonMutuals.filter(u=> S.uf.selected.has(u.username));if(!queue.length)return;S.running=true;S.stop=false;S.uf.phase='running';UI.uf.showRunning(queue.length);UF.run(queue,(cur,total,user,remMs)=> UI.uf.updateProgress(cur,total,user,remMs),(remMs,cur,total)=> UI.uf.updateCooldown(remMs,cur,total)).then(({done,stopped})=>{S.running=false;S.uf.phase='done';UI.uf.showDone(done,stopped);});},startAutoFollow(){S.activeModule='autofollow';S.running=false;S.stop=false;S.af.candidates=[];S.af.selected.clear();S.af.seen.clear();S.af.count=0;S.af.phase='config';S.af.scanDone=false;document.getElementById('xuf-tab-af')?.classList.add('xuf-tab-active');document.getElementById('xuf-tab-uf')?.classList.remove('xuf-tab-active');UI.af.showConfig();},async loadFollowers(targetCount){S.af.targetCount=targetCount;S.af.candidates=[];S.af.seen.clear();S.af.scanDone=false;S.running=true;S.stop=false;S.af.phase='loading';UI.af.showLoading(0,targetCount);const loaded=await SCRAPER.scanFollowers(targetCount,(n,t)=>{UI.af.updateLoading(n,t);});if(S.stop){CTRL.backToDashboard();return;}S.running=false;S.af.phase='results';S.af.selected.clear();S.af.candidates.forEach(u=> S.af.selected.add(u.username));UI.af.showResults(S.af.candidates,targetCount);},runAutoFollow(){const queue=S.af.candidates.filter(u=> S.af.selected.has(u.username));if(!queue.length)return;S.running=true;S.stop=false;S.af.phase='running';UI.af.showRunning(queue.length);AF.run(queue,(cur,total,user,remMs)=> UI.af.updateProgress(cur,total,user,remMs),(remMs,cur,total)=> UI.af.updateCooldown(remMs,cur,total)).then(({done,stopped})=>{S.running=false;S.af.phase='done';UI.af.showDone(done,stopped);});},};UI.mount();CTRL.init();console.log('%cUnfollowers-X V2.2 cargado','color:#484848;font-weight:bold;font-size:13px');})();